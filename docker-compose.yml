version: '3.8'
services:
  vincentbuilds:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./runtime:/app/runtime
      - ./vector_index:/app/vector_index
      - ./data:/app/data
    restart: unless-stopped
